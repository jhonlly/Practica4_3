<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.majada.jhona.practica4_3">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Alta" />
        <activity android:name=".ConsultaCodigo" />
        <activity android:name=".ConsultarDescripcion" />
        <activity android:name=".BajaCodigo" />
        <activity android:name=".Modificar" />
        <activity android:name=".VerRegistros"></activity>
    </application>

</manifest>